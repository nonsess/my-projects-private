# Журнал сессий

Append-only. Каждая сессия — одна запись `## YYYY-MM-DD` + 1–3 строки: длительность, о чём, настроение.

Не удалять. Не редактировать прошлые записи.

## 2026-06-20

~4ч+ (автономная сессия, продолжение). Флажок v2: безопасность (rate limit, webhook верификация, checkout bug, secure cookie, закрыты порты, убран --reload), полный редизайн лендинга (тёмный герой, dot-grid, glassmorphism DemoCard, тикер, монотипные eyebrow, анимации), 404 страница, success страница, FilterForm валидация. TypeScript чистый.

## 2026-06-20

~3ч (автономная ночная сессия). Трекер недвижимости «Флажок» — полный UX-рефакторинг фронтенда: продающий лендинг (PAS/StoryBrand), shadcn/ui + Lucide на всех экранах, skeleton loading везде, toast (sonner) вместо alert/confirm, клиентская фильтрация объявлений (цена, площадь, сортировка × 4), copy URL на карточках, source badge, отмена подписки с confirm-toast, skeleton на settings. TypeScript чистый. Даниил ушёл спать — работал автономно. Продуктивно, без блокеров.

## 2026-05-23 (продолжение сессии 09f6b817, ч. 2)
~2ч (resumed). Продолжение сессии 2026-05-22: откат PDF-файлов из git-истории [[изоком]] через force push (`git reset --hard f8d06ca`), `.gitignore` возвращён к `data/` без исключений, `Dockerfile.prod` и `compose.yaml` без document-слоя. Второй раунд CI-фикса: добавлен `permissions: contents: read` на уровне воркфлоу (коммит `5e33323`) — `changes`/`lint` jobs получали implicit `contents: none`, checkout падал. Финальный статус-чек: «какие у меня таски?» → трекер пустой. Сфокусированный, деловой.

## 2026-05-23
~3ч. Задача #29 [[изоком]] — полный цикл разработки contact_collector: брейншторм (LangGraph interrupt() vs stateful flag), дизайн-спек, план реализации, имплементация 6 файлов через subagent-driven-development (10 коммитов), интеграционный тест на живом стеке. Все 3 флоу прошли: escalation+контакт, escalation+отказ, general. Запушено (1c7a42f). Деловой, технически сфокусированный.

## 2026-05-22 (статус-чек, ~5 мин)
Краткий запрос: «Какие у меня таски и открытые изоком-задачи без ответственных?». Запрошен MCP-трекер: активная задача #29 (contact_collector, до 2026-05-27), #26 у Ильи (ESCALATION_TG_CHAT_ID, дедлайн завтра), #27/#28 — done. Открытых без ответственных нет. Параллельно запущены два reconcile-агента по несверённым транскриптам. Нейтральный.

## 2026-05-22 (продолжение сессии, ч. 2)
~14ч. Задача #35 [[изоком]]: фикс URL документов на фронте (`backend:8000//documents/download` → `/api/documents/download` через `VITE_BASE_URL`). CI/CD фикс [[новый-изоком-ci-permissions]]: permissions в GitHub Actions переехали с воркфлоу-уровня на уровень джоба (workflow-level `contents: read` блокировал `packages: write`). Коммит f0e466c, запушено. Таски #32/#33/#34 удалены (созданы по ошибке). Деловой, сфокусированный.

## 2026-05-22
~4ч 17мин. Задача #28 [[изоком]] — полный цикл: фикс потери контекста в трёх нодах агента (completeness_check, general_response, router — плоская строка → чередующиеся user/assistant). Проверено на живом Docker-стеке (Redis + GPT-4o). Закоммичено (359a07b), запушено. Задача #28 → done в командном трекере. Залито в Outline + LightRAG (job 47). Деловой, технически вовлечённый, нетерпелив к паузам.

## 2026-05-21
~2 часа. Проверка запуска дедуктивного мышления (хуки рабочие, `vault/insights/log.md` создан). Диф с апстримом `/coding/bba-private-assistant`. Глубокий разбор [[изоком]] задачи #28 — бот не помнит контекст: выяснилось, что проблема не только в TG, но и на веб-клиенте (бэк один). Корневая причина — LangGraph State без `add_messages` reducer. Добавлен комментарий к задаче #28. В конце — привязка telegram_id=1092889318 к пользователю Даниил (id=6 в BBA), сессия оборвалась на последнем шаге. Деловой, сфокусированный.

## 2026-05-18
Индексация базы знаний [[изоком-rag-индексация]] в Qdrant: 5235 чанков в двух коллекциях (нормативы + продукты/доки). Написан `ingest_docs.py`. Нюанс: Ollama надо поднять с `OLLAMA_HOST=0.0.0.0` для Docker. Рабочий день, сосредоточенно.

## 2026-05-15
Первая сессия. Онбординг (личный блок): Даниил, fullstack-разработчик bb-academy (TypeScript/React/Next.js + Python/FastAPI/Django/PostgreSQL/MongoDB/Redis). Проектный блок пропущен. Фокус напарника — код и техзадачи.

## 2026-06-07
Стратегическая сессия: Даниил рассказал о себе подробнее — цель выйти на доход через продажу разработки, Кави почти готов (ждёт предоплату). Обсуждение конвейера: Кворк vs продуктизация Кави. Открытые вопросы: статус предоплаты, степень кастомизации Кави. Деловой, стратегически настроенный.

## 2026-05-26
~17 мин. Задача [[изоком]]: добавить эскалацию в мессенджер МАКС параллельно с Telegram. Изучил MAX Bot API, требования к серверу вне РФ (технических блоков нет, нужно верифицированное юрлицо РФ для регистрации бота). Добавил `MaxSettings`, `send_to_manager_max()` в `notify.py`, `asyncio.gather` в `notify_manager.py`, переменные в `.env.example`. Закоммичено `c6c49d7`, запушено. Сессия оборвалась на rate_limit. Деловой, целеориентированный.

## 2026-06-14
~5 часов. Плотная кодовая сессия по [[kavi]]. Слоты времени доставки с иркутским временем, блокировка вне часов работы. Дашборд и хиты только для завершённых заказов + cancel rate. Уведомления: браузер-beep для админа и клиента. PWA манифест, иконки, service worker. Web Push через VAPID + pywebpush — миграция push_subscriptions накатана. SEO: JSON-LD Restaurant + FAQPage, аккордион FAQ, sitemap/robots. Юридика: /privacy, /offer, согласие под кнопкой. Обновлён футер с реальными контактами. Деловой, технически насыщенный.
